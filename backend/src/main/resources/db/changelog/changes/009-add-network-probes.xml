<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="009-add-network-probes" author="stimur1709">
        <sql dbms="postgresql">
            ALTER TABLE tasks DROP CONSTRAINT tasks_type_check;
            ALTER TABLE tasks ADD CONSTRAINT tasks_type_check
                CHECK (type IN ('HTTP_CHECK', 'PORT_CHECK', 'DNS_CHECK', 'PING', 'TLS_CHECK'));
            ALTER TABLE monitors DROP CONSTRAINT monitors_type_check;
            ALTER TABLE monitors ADD CONSTRAINT monitors_type_check
                CHECK (type IN ('HTTP_CHECK', 'PORT_CHECK', 'DNS_CHECK', 'PING', 'TLS_CHECK'));
        </sql>
        <rollback>
            DELETE FROM monitoring_results
                WHERE monitor_id IN (SELECT id FROM monitors WHERE type IN ('DNS_CHECK', 'PING', 'TLS_CHECK'));
            DELETE FROM monitors WHERE type IN ('DNS_CHECK', 'PING', 'TLS_CHECK');
            DELETE FROM tasks WHERE type IN ('DNS_CHECK', 'PING', 'TLS_CHECK');
            ALTER TABLE tasks DROP CONSTRAINT tasks_type_check;
            ALTER TABLE tasks ADD CONSTRAINT tasks_type_check CHECK (type IN ('HTTP_CHECK', 'PORT_CHECK'));
            ALTER TABLE monitors DROP CONSTRAINT monitors_type_check;
            ALTER TABLE monitors ADD CONSTRAINT monitors_type_check CHECK (type IN ('HTTP_CHECK', 'PORT_CHECK'));
        </rollback>
    </changeSet>
</databaseChangeLog>
