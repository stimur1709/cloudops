<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="013-add-run-command" author="stimur1709">
        <sql dbms="postgresql">
            DELETE FROM outbox_messages
            WHERE aggregate_type = 'TASK'
              AND aggregate_id IN (SELECT id FROM tasks WHERE type = 'TEST_OPERATION');
            DELETE FROM tasks WHERE type = 'TEST_OPERATION';
        </sql>
        <addColumn tableName="tasks">
            <column name="parameters" type="JSONB" defaultValueComputed="'{}'::jsonb">
                <constraints nullable="false"/>
            </column>
        </addColumn>
        <rollback>
            <dropColumn tableName="tasks" columnName="parameters"/>
        </rollback>
    </changeSet>
</databaseChangeLog>
